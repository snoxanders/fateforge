{
  "name": "fateforge-root",
  "private": true,
  "scripts": {
    "postinstall": "prisma generate --schema=./backend/prisma/schema.prisma && cd backend && npm install && cd frontend && npm install",
    "build": "cd backend && npx prisma generate && npm run build && cd frontend && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@supabase/supabase-js": "^2.87.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
